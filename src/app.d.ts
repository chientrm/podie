declare namespace App {
	interface Locals {
		user: import('@sentry/types').User | undefined;
	}
	// interface PageData {}
	// interface Platform {}
	// interface PrivateEnv {}
	// interface PublicEnv {}
}
